import { LoadSceneOptions, LoadedData } from "./loadScene";

type FetchSceneAndMount_scene_sphere = (
  options?: LoadSceneOptions
) => Promise<LoadedData>;

export const fetchSceneAndMount_scene_sphere: FetchSceneAndMount_scene_sphere;
